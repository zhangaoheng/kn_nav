# 统一配置修改记录

分支：`dev/unified-navigation-config`

## 已完成

- 为 A2、Go2、Go2-W 和本地环境建立统一的 `navigation.yaml`；
- 启动文件改为从统一 YAML 加载各节点参数；
- 支持命令行覆盖导航模式、网卡和启动开关；
- 地图切换统一管理 PCD 和 Tomogram；
- 增加配置检查脚本和旧配置迁移脚本；
- 补充统一配置测试及使用文档；
- 增加《新机械狗部署参数修改指南》；
- 修复 tmux 开启鼠标后无法正常拖选复制的问题；
- tmux 启动后在右上角窗格自动运行 Web API；
- 增加 `GET /api/maps`，查询统一配置中的全部可切换地图名称；
- 创建 `config_v2` 分层配置目录及首版 A2 配置草案；
- 按《使用.md》补充 `config_v2` 的实机部署可调参数；
- 将 A2、local、Go2 和 Go2-W 四套现有配置迁入 `config_v2`。

## 已检查

- A2 的 `navigation.yaml` 通过配置检查；
- tmux 启动脚本通过 Bash 语法检查；
- tmux 修改通过 `git diff --check`。
