# 局部规划安全修改工作日志

## 已完成

1. 保留 PCT 路径高度
   - 取消 `applyLinearZReference()` 线性高度覆盖。
   - 局部规划订阅原始 `/pct_path`，按照当前局部路径段恢复 Z 高度变化。

2. 完整轨迹碰撞检查
   - 初始化、优化和细化阶段的碰撞检查由前 `2/3` 扩展到整条轨迹。
   - 轨迹发布前增加全轨迹碰撞校验；检测到占用时拒绝发布。
   - 执行期间检查全部剩余轨迹。

3. PCT 路径走廊约束
   - 将绿色 PCT 路径作为局部 B 样条优化参考走廊。
   - 超出走廊时增加优化代价，发布前再次进行最大偏离距离硬校验。
   - A2 默认最大偏离距离为 `0.6 m`，走廊权重为 `5.0`。

## 验证

- 三个核心 C++ 实现文件已通过离线语法检查。
- 完整 `colcon build` 被项目现有的 `path_searching` CMake 依赖导出问题阻断。
- `git diff --check` 通过；未启动导航，未向机械狗发送运动命令。
