# 1.docker 部署
* 适配x86的镜像制作file已经在git仓库的docker文件夹中，可以使用`docker build`命令制作镜像，适配arm系统的dockerfile未给出，需后续补充
* 可以使用`run_container.sh`启动镜像，容器自动设置为`unless-stopped`模式，启动后不会自动执行任何命令，关机后会自动重启
* 当前的dockerfile，导航的代码，unitreesdk，livoxsdk会在制作镜像时从git拉取，但是livox_ros_driver需要从本地复制,open3d的第三方库也是从本地的thirdparty复制，且open3d的第三方库也分arm和x86的版本
* 可以使用`docker load 和 docker save`命令将镜像导入或导出

# 2.地图准备
* 1. 需要pct-planner处理得到的`.pickle`格式的全局地图用于全局规划

* 2. 需要用于全局定位的pcd地图，建议voxel_size = 0.2，可以在使用扫描仪配套的软件时，选择采样大小为0.2并导出

# 3.主要参数调整


```yaml   
    # /open3d_loc.yaml中 
    # 搜索点的范围，越大icp的得分越高
    icp_distance_threshold: 0.20
    fitness_eval_threshold: 0.20

    # 跟踪定位的icp阈值，越小越容易接受本次icp结果
    threshold_fitness: 0.8
    # 初始化重定位的icp阈值，越小越容易接受本次icp结果
    threshold_fitness_init: 0.9

     # 跟踪时单次icp的最大位移距离
    max_icp_translation: 0.4
     # 跟踪时单次icp的最大旋转角度
    max_icp_yaw_deg: 2.0
     # 初始重定位单次icp的最大位移距离
    max_init_icp_translation: 20.0
     # 初始重定位单次icp的最大旋转角度
    max_init_icp_yaw_deg: 180.0
    
    # scan_planner.yaml中
    # 局部规划的前向视距
    fsm.planning_horizon: 3.5

    # 滑动地图的大小
    grid_map.sliding_map_size_x: 10.0
    grid_map.sliding_map_size_y: 10.0
    grid_map.sliding_map_size_z: 5.0

    #滑动地图更新范围
    grid_map.local_update_range_x: 5.5
    grid_map.local_update_range_y: 5.5
    grid_map.local_update_range_z: 3.0
    grid_map.map_sliding_en: true
    grid_map.map_sliding_thresh: 0.2

    # 机器狗双圆柱模型
    grid_map.double_cylinder_radius: 0.28
    grid_map.double_cylinder_offset: 0.23

    # 障碍物上下膨胀
    grid_map.obstacles_inflation_z_up: 0.1
    grid_map.obstacles_inflation_z_down: 0.1

    # go2_bridge.yaml中
    # 机器狗心跳话题
    sport_state_topic: rt/sportmodestate
```
 4.具体使用
 ```bash
    # 启动livox雷达驱动
    ros2 launch livox_ros_driver2 msg_MID360s_launch.py

    # 启动导航
    ros2 launch pct_scan_navigation unitree_go2w_pct_scan_navigation.launch.py

    # 临时打点的程序
    python3 /home/code/work_space/kn_nav/src/tools/goal_points_cli.py

    # 底盘速度桥接
    ros2 service call /go2_cmd_vel_bridge/enable std_srvs/srv/SetBool '{data: true}'
```